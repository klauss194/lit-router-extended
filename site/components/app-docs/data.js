export const ducuments = {
  "tutorials": {
    "order": "1",
    "title": "Lit Router Extended",
    "description": "quickstart guide with tutorial",
    "sections": [
      {
        "id": "installation",
        "title": "Installation & Basic Setup",
        "order": 1,
        "category": "tutorials",
        "html": "<h2>Installation</h2>\n<p>Install lit-router via npm. The package has no external dependencies.</p>\n<copy-console content=\"npm install lit-router\"></copy-console>\n<h2>Basic Setup</h2>\n<p>Create a <code>Router</code> instance inside your LitElement. <code>Router</code> is a <a href=\"https://lit.dev/docs/composition/controllers/\">Reactive Controller</a> — it manages route matching and triggers re-renders when the URL changes.</p>\n<pre><code class=\"language-typescript\">import { LitElement, html } from &#39;lit&#39;;\nimport Router from &#39;lit-router&#39;;\n\nclass MyApp extends LitElement {\n  _router = new Router(this, [\n    { path: &#39;/&#39;, render: () =&gt; html`&lt;home-page&gt;&lt;/home-page&gt;` },\n    { path: &#39;/users/:id&#39;, render: ({ params }) =&gt;\n      html`&lt;user-profile id=${params.id}&gt;&lt;/user-profile&gt;` },\n  ]);\n\n  render() {\n    return html`\n      &lt;nav&gt;\n        &lt;a href=&quot;/&quot;&gt;Home&lt;/a&gt;\n        &lt;a href=&quot;/users/123&quot;&gt;Profile&lt;/a&gt;\n      &lt;/nav&gt;\n      &lt;main&gt;\n        ${this._router.outlet()}\n      &lt;/main&gt;\n    `;\n  }\n}\n\ncustomElements.define(&#39;my-app&#39;, MyApp);\n</code></pre>\n<p>The <code>Router</code> intercepts all <code>&lt;a&gt;</code> clicks on <code>window</code> automatically.</p>\n<h2>Rendering Outlets</h2>\n<p>To render the current matched route, call the controller&#39;s <code>outlet()</code> method inside the host LitElement&#39;s <code>render</code> function:</p>\n<pre><code class=\"language-javascript\">render() {\n  return html`&lt;main&gt;${this._router.outlet()}&lt;/main&gt;`;\n}\n</code></pre>\n<p>This generates a <code>&lt;lit-router-outlet&gt;</code> wrapper that renders your matched components and propagates lifecycle events.</p>\n"
      },
      {
        "id": "getting-started",
        "title": "Getting Started",
        "order": 2,
        "category": "tutorials",
        "html": "<h2>Getting Started</h2>\n<p>Welcome to <code>lit-router</code>! This guide will walk you through setting up a basic application, defining your first routes, and using our reactive controllers to manage navigation seamlessly.</p>\n<p><strong>Let&#39;s start by creating your application&#39;s root component and instantiating the main <code>Router</code></strong>.</p>\n<p>The <code>Router</code> is a reactive controller that listens to URL changes and determines which view to display. You define your routes as an array of objects, each containing a <code>path</code> and a <code>render</code> callback. Finally, you use the <code>outlet()</code> method inside your component&#39;s template to render the matched route.</p>\n<blockquote>\n<p>index.js</p>\n</blockquote>\n<pre><code class=\"language-javascript\">import {html, LitElement} from &quot;lit&quot;;\nimport {Router} from &#39;lit-router-extended&#39;;\n\nexport class AppRoot extends LitElement {\n    router = new Router(this, [\n        { path: &#39;/&#39;, render: () =&gt; html`&lt;div&gt;&lt;/div&gt;` },\n        { path: &#39;/about&#39;, render: () =&gt; html`&lt;div&gt;About content&lt;/div&gt;` },\n        { path: &#39;*&#39;, render: () =&gt; html`&lt;strong&gt;Page Not Found&lt;/strong&gt;` },\n    ]);\n\n    render() {\n        return html`\n            &lt;nav&gt;\n                &lt;ul&gt;\n                    &lt;a href=&quot;/&quot;&gt;&lt;li&gt;Home&lt;/li&gt;&lt;/a&gt;\n                    &lt;a href=&quot;/about&quot;&gt;&lt;li&gt;About&lt;/li&gt;&lt;/a&gt;\n                    &lt;a href=&quot;/foo&quot;&gt;&lt;li&gt;Undefined Route&lt;/li&gt;&lt;/a&gt;\n                &lt;/ul&gt;\n            &lt;/nav&gt;\n            \n            &lt;main&gt;${this._router.outlet()}&lt;/main&gt;\n        `;\n    }\n}\ncustomElements.define(&#39;app-root&#39;, AppRoot);\n</code></pre>\n<p>Next, mount your root component in your HTML entry point just like any standard Web Component. The <code>Router</code> will automatically take over window.history and evaluate the current <code>URL</code>.</p>\n<blockquote>\n<p>index.html</p>\n</blockquote>\n<pre><code class=\"language-html\">&lt;html lang=&quot;en&quot;&gt;\n&lt;head&gt;\n    &lt;script src=&quot;/index.js&quot; type=&quot;text/javascript&quot; /&gt;\n&lt;/head&gt;\n&lt;body&gt;\n    &lt;app-root&gt;&lt;/app-root&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre>\n<h3>Contextual Routing with the Navigation Controller</h3>\n<p>While standard anchor tags (<code>&lt;a href=&quot;...&quot;&gt;</code>) work perfectly fine, real-world applications often require programmatic navigation and active-state management for menus.\nLet&#39;s extract our navigation into a dedicated component. By instantiating the <strong><code>Navigation</code> controller</strong>, we can programmatically navigate using the <code>navigate()</code> method and easily react to URL changes (<strong>like applying an active class to the current route</strong>).</p>\n<blockquote>\n<p>nav-component.js</p>\n</blockquote>\n<pre><code class=\"language-javascript\">import {html, LitElement} from &quot;lit&quot;;\nimport {classMap} from &quot;lit-html/directives/class-map.js&quot;;\nimport {Navigation} from &#39;lit-router-extended&#39;;\n\nexport class AppNav extends LitElement {\n    navigation = new Navigation(this);\n    \n    connectedCallback() {\n        super.connectedCallback();\n        this.addEventListener(Navigation.event, this.updateOnRouterChange);\n    }\n    \n    disconnectedCallback() {\n        super.disconnectedCallback();\n        this.removeEventListener(Navigation.event, this.updateOnRouterChange);\n    }\n\n    updateOnRouterChange = () =&gt; {\n        this.requestUpdate();\n    }\n\n    render() {\n        return html`\n            &lt;nav&gt;\n                &lt;ul&gt;\n                    &lt;li&gt;\n                      &lt;button \n                        class=${classMap({&quot;active&quot;: this.navigation.pathname === &quot;/&quot; })} \n                        @click=${() =&gt; this.navigation.navigate(&quot;/&quot;)} \n                      &gt;Home&lt;/button&gt;\n                    &lt;/li&gt;\n                    &lt;li&gt;\n                      &lt;button \n                        class=${classMap({&quot;active&quot;: this.navigation.pathname === &quot;/about&quot; })} \n                        @click=${() =&gt; this.navigation.navigate(&quot;/about&quot;)} \n                      &gt;About&lt;/button&gt;\n                    &lt;/li&gt;\n                    &lt;li&gt;\n                      &lt;button \n                        class=${classMap({&quot;active&quot;: this.navigation.pathname === &quot;/foo&quot; })} \n                        @click=${() =&gt; this.navigation.navigate(&quot;/foo&quot;)} \n                      &gt;Foo&lt;/button&gt;\n                    &lt;/li&gt;\n                &lt;/ul&gt;\n            &lt;/nav&gt;\n        `;\n    }\n}\n\ncustomElements.define(&#39;app-nav&#39;, AppNav);\n</code></pre>\n<p>Now, let&#39;s head back to <code>index.js</code> to implement our newly created <code>&lt;app-nav&gt;</code> component. This keeps our <code>AppRoot</code> clean and modular.</p>\n<blockquote>\n<p>index.js</p>\n</blockquote>\n<pre><code class=\"language-javascript\">// .. rest of imports\n\nimport &#39;./nav-component.js&#39;;\n\nexport class AppRoot extends LitElement {\n    //... rest of code\n    render() {\n        return html`\n            &lt;app-nav&gt;&lt;/app-nav&gt;\n            &lt;main&gt;${this._router.outlet()}&lt;/main&gt;\n        `;\n    }\n}\n</code></pre>\n<h3>Handling Imperative Navigation (&quot;Go Back&quot;)</h3>\n<p>The <code>Navigation</code> controller isn&#39;t just for moving forward; it can also interact with browser history.\nLet&#39;s create an <code>AppAbout</code> component to demonstrate how to trigger a programmatic <strong>&quot;back&quot;</strong> action using <code>this.navigation.goback()</code>. This is particularly useful for building custom UI elements like back buttons in mobile-like interfaces.</p>\n<blockquote>\n<p>about-component.js</p>\n</blockquote>\n<pre><code class=\"language-javascript\">import {html, LitElement} from &quot;lit&quot;;\nimport {classMap} from &quot;lit/directives/class-map.js&quot;;\nimport {Navigation} from &#39;lit-router-extended&#39;;\n\nexport class AppAbout extends LitElement {\n    navigation = new Navigation(this);\n\n    render() {\n        return html`\n            &lt;h1&gt;About&lt;/h1&gt;\n            &lt;p&gt;\n                Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry&#39;s standard dummy text ever since 1966, when designers at Letraset and James Mosley, the librarian at St Bride Printing Library in London, took a 1914 Cicero translation and scrambled it to make dummy text for Letraset&#39;s Body Type sheets. It has survived not only many decades, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised thanks to these sheets and more recently with desktop publishing software like Aldus PageMaker and Microsoft Word including versions of Lorem Ipsum.\n            &lt;/p&gt;\n            &lt;div&gt;\n               &lt;button @click=${() =&gt; this.navigation.goback()}&gt;Go Back&lt;/button&gt;\n            &lt;/div&gt;\n        `;\n    }\n}\n\ncustomElements.define(&#39;app-about&#39;, AppAbout);\n</code></pre>\n<p>Finally, let&#39;s update our <code>AppRoot</code> again. We will inject the <code>&lt;app-nav&gt;</code> directly into the <code>Home</code> route&#39;s render callback, and assign our new <code>&lt;app-about&gt;</code> component to the <strong>/about</strong> path.</p>\n<blockquote>\n<p>index.js</p>\n</blockquote>\n<pre><code class=\"language-javascript\">// ... rest of imports\nimport &quot;./about-component.js&quot;\n\nexport class AppRoot extends LitElement {\n    router = new Router(this, [\n        { path: &#39;/&#39;, render: () =&gt; html`\n            &lt;app-nav&gt;&lt;/app-nav&gt;\n            &lt;div&gt;&lt;h1&gt;Home Page&lt;/h1&gt;&lt;/div&gt;`\n        },\n        { path: &#39;/about&#39;, render: () =&gt; html`&lt;app-about&gt;&lt;/app-about&gt;` },\n        { path: &#39;*&#39;, render: () =&gt; html`\n            &lt;strong&gt;Page Not Found&lt;/strong&gt;\n            &lt;a href=&quot;/&quot;&gt;go to home&lt;/a&gt;`\n        },\n    ]);\n\n    render() {\n        return html`\n            &lt;main&gt;${this.router.outlet()}&lt;/main&gt;\n        `;\n    }\n}\n</code></pre>\n<h3>Nested Routing (Sub-Routes)</h3>\n<p>Large applications often require nested layouts, such as a dashboard with its own internal navigation. <code>lit-router</code> makes this seamless through structural <strong>DOM nesting</strong>.\nTo create nested routes, the parent route must end with a wildcard (<code>*</code>). This tells the <code>Router</code> to capture the remaining URL (the &quot;tail&quot;) and pass it down to any child <code>Routes</code> controller inside its outlet.\nLet&#39;s create an <code>AppDashboard</code> component that manages its own sub-routes using the <code>Routes</code> controller (notice it&#39;s Routes, not <code>Router</code>).</p>\n<blockquote>\n<p>dashboard-component.js</p>\n</blockquote>\n<pre><code class=\"language-javascript\">import { html, LitElement } from &quot;lit&quot;;\nimport { Routes } from &quot;lit-router-extended&quot;;\n\nexport class AppDashboard extends LitElement {\n    // The child controller handling the URL tail\n    routes = new Routes(this, [\n        { path: &#39;/&#39;, render: () =&gt; html`&lt;h3&gt;Dashboard Overview&lt;/h3&gt;` },\n        { path: &#39;/settings&#39;, render: () =&gt; html`&lt;h3&gt;Dashboard Settings&lt;/h3&gt;` },\n        { path: &#39;*&#39;, render: () =&gt; html`&lt;h3&gt;Section Not Found&lt;/h3&gt;` },\n    ]);\n\n    render() {\n        return html`\n            &lt;h2&gt;Dashboard Layout&lt;/h2&gt;\n            &lt;nav&gt;\n                &lt;!-- Relative or absolute links work perfectly --&gt;\n                &lt;a href=&quot;/dashboard&quot;&gt;Overview&lt;/a&gt;\n                &lt;a href=&quot;/dashboard/settings&quot;&gt;Settings&lt;/a&gt;\n            &lt;/nav&gt;\n            \n            &lt;section class=&quot;dashboard-content&quot;&gt;\n                ${this.routes.outlet()}\n            &lt;/section&gt;\n        `;\n    }\n}\n\ncustomElements.define(&#39;app-dashboard&#39;, AppDashboard);\n</code></pre>\n<p>Now, we register the parent route in our <code>AppRoot</code> using the wildcard syntax:</p>\n<blockquote>\n<p>index.js</p>\n</blockquote>\n<pre><code class=\"language-javascript\">// ... previous imports\nimport &quot;./dashboard-component.js&quot;;\n\nexport class AppRoot extends LitElement {\n    router = new Router(this, [\n        // ... previous routes\n        \n        // IMPORTANT: The wildcard (*) allows tail propagation to AppDashboard\n        { path: &#39;/dashboard/*&#39;, render: () =&gt; html`&lt;app-dashboard&gt;&lt;/app-dashboard&gt;` },\n    ]);\n    // ... rest of the component\n}\n</code></pre>\n<h3>Route-Driven Modals</h3>\n<p>A common UI requirement is opening a modal that has its own <strong>distinct URL</strong>, allowing users to share the link or use the browser&#39;s <strong>&quot;Back&quot;</strong> button to close it.\nWe can achieve this beautifully by combining native HTML <code>&lt;dialog&gt;</code> elements, nested routing, and the <code>push()</code> / <code>pop()</code> methods from the Navigation controller.\nLet&#39;s create an <code>AppUsers</code> component that lists users. When a user is clicked, we <strong>push</strong> a new relative route. The enter hook opens the modal, and the <code>pop()</code> method safely closes it, restoring the parent URL.</p>\n<blockquote>\n<p>users-component.js</p>\n</blockquote>\n<pre><code class=\"language-javascript\">import { html, LitElement, nothing } from &quot;lit&quot;;\nimport { ref, createRef } from &quot;lit/directives/ref.js&quot;;\nimport { Routes, Navigation } from &quot;lit-router-extended&quot;;\n\nexport class AppUsers extends LitElement {\n    navigation = new Navigation(this);\n    dialogRef = createRef();\n\n    modalRoutes = new Routes(this, [\n        {\n            // Matches /users/:id\n            path: &#39;/:id&#39;,\n            enter: () =&gt; {\n                // Open the native HTML dialog when the route is hit\n                this.dialogRef.value?.showModal();\n            },\n            render: (ctx) =&gt; html`\n                &lt;h3&gt;User Profile&lt;/h3&gt;\n                &lt;p&gt;Viewing details for user ID: &lt;strong&gt;${ctx.params.id}&lt;/strong&gt;&lt;/p&gt;\n            `\n        },\n        {\n            // Fallback: If there&#39;s no tail (just /users), close the modal\n            path: &#39;*&#39;,\n            render: () =&gt; {\n                this.dialogRef.value?.close();\n                return nothing;\n            }\n        }\n    ]);\n\n    // Handle native close events (like pressing the ESC key)\n    onDialogClose = () =&gt; {\n        if (this.dialogRef.value?.returnValue === &#39;dismiss&#39;) {\n            this.navigation.pop(); // Reverts the URL tail automatically\n        }\n    }\n\n    render() {\n        return html`\n            &lt;h2&gt;Users Directory&lt;/h2&gt;\n            \n            &lt;ul&gt;\n                &lt;!-- push() appends the relative path to the current URL --&gt;\n                &lt;li&gt;\n                    &lt;button @click=${() =&gt; this.navigation.push(&#39;./101&#39;)}&gt;\n                        View User 101\n                    &lt;/button&gt;\n                &lt;/li&gt;\n                &lt;li&gt;\n                    &lt;button @click=${() =&gt; this.navigation.push(&#39;./202&#39;)}&gt;\n                        View User 202\n                    &lt;/button&gt;\n                &lt;/li&gt;\n            &lt;/ul&gt;\n\n            &lt;!-- Native HTML Dialog --&gt;\n            &lt;dialog ${ref(this.dialogRef)} @close=${this.onDialogClose}&gt;\n                &lt;div class=&quot;modal-content&quot;&gt;\n                    ${this.modalRoutes.outlet()}\n                &lt;/div&gt;\n                &lt;form method=&quot;dialog&quot;&gt;\n                    &lt;button value=&quot;dismiss&quot;&gt;Close&lt;/button&gt;\n                &lt;/form&gt;\n            &lt;/dialog&gt;\n        `;\n    }\n}\n\ncustomElements.define(&#39;app-users&#39;, AppUsers);\n</code></pre>\n<p>To test this, just add <code>{ path: &#39;/users/*&#39;, render: () =&gt; html&lt;app-users&gt;&lt;/app-users&gt;}</code> to your root router. Notice how <code>push(&#39;./101&#39;)</code> changes the URL to <strong>/users/101</strong> and opens the modal. Clicking the &quot;Close&quot; button triggers a form submission that closes the <code>dialog</code> natively, triggering <code>@close</code>, which safely evaluates <code>pop()</code> to strip <strong>/101</strong> from the URL without triggering a full page reload!</p>\n"
      }
    ]
  },
  "reference": {
    "order": "3",
    "title": "Reference",
    "description": "API reference",
    "sections": [
      {
        "id": "router-api",
        "title": "Router API Reference",
        "order": 1,
        "category": "reference",
        "html": "<h2>Router Instance</h2>\n<p>Access the root <code>Router</code> instance via <code>this.nav.router</code> (if using the Navigation controller) or <code>this._router</code> (from the owning component).</p>\n<h3>Methods</h3>\n<ul>\n<li><strong><code>subscribe(callback)</code></strong>: Subscribes to route state changes. Returns an unsubscribe function.</li>\n<li><strong><code>replaceState(options)</code></strong>: Updates the current URL&#39;s query string, hash, or extra state via <code>history.replaceState</code> <em>without</em> triggering navigation.</li>\n<li><strong><code>buildUrl(pathname)</code></strong>: Constructs a full URL string for a given pathname, resolved against the current route.</li>\n<li><strong><code>goto(pathname, options?)</code></strong>: Resolves a pathname and activates the matching route <em>without</em> updating the URL bar.</li>\n<li><strong><code>navigate(pathname, options?)</code></strong>: Resolves a pathname, activates the matching route, and updates the URL bar via <code>history.pushState</code>.</li>\n</ul>\n<h2>Custom Events</h2>\n<p>The <code>Router</code> dispatches events on <code>window</code> during navigation.</p>\n<ul>\n<li><strong><code>lit-router-location-changing</code></strong>: Fires <strong>before</strong> navigation starts. Cancelable via <code>e.preventDefault()</code>. Detail: <code>{ currentPathname, pathname, extraParams, searchParams, hash }</code>.</li>\n<li><strong><code>lit-router-location-changed</code></strong>: Fires <strong>after</strong> navigation completes. Detail: <code>{ pathname, params, extraParams, searchParams, hash }</code>.</li>\n<li><strong><code>lit-router-error</code></strong>: Fires on <code>&lt;a&gt;</code> elements when an anchor click causes an error. Detail: <code>{ url, error }</code>.</li>\n</ul>\n<h2>Error Types</h2>\n<ul>\n<li><strong><code>InvalidNavigationError</code></strong>: Thrown when navigation is cancelled by hooks, no routes are defined, or <code>push()</code> is called invalidly. Swallowed silently during <code>&lt;a&gt;</code> clicks.</li>\n<li><strong><code>RouteNotFoundError</code></strong>: Thrown when no route matches the target pathname.</li>\n</ul>\n"
      },
      {
        "id": "navigation-controller",
        "title": "Navigation Controller API",
        "order": 2,
        "category": "reference",
        "html": "<p><code>Navigation</code> is a Lit Reactive Controller that gives components access to the router without being a route itself.</p>\n<pre><code class=\"language-typescript\">import { Navigation } from &#39;lit-router&#39;;\nclass UserProfile extends LitElement {\n  nav = new Navigation(this);\n}\n</code></pre>\n<h2>Reactive Properties</h2>\n<ul>\n<li><code>this.nav.url</code> — e.g., <code>&quot;/users/42?tab=posts#bio&quot;</code></li>\n<li><code>this.nav.pathname</code> — e.g., <code>&quot;/users/42&quot;</code></li>\n<li><code>this.nav.params</code> — e.g., <code>{ id: &quot;42&quot; }</code></li>\n<li><code>this.nav.searchParams</code> — e.g., <code>{ tab: &quot;posts&quot; }</code> (has setter)</li>\n<li><code>this.nav.hash</code> — e.g., <code>&quot;bio&quot;</code> (has setter)</li>\n<li><code>this.nav.extraParams</code> — e.g., <code>{ draft: &quot;abc&quot; }</code></li>\n<li><code>this.nav.routeName</code> — e.g., <code>&quot;user-detail&quot;</code></li>\n</ul>\n<h2>Methods</h2>\n<ul>\n<li><strong><code>navigate(pathname, options?)</code></strong>: Navigates to an absolute application path.</li>\n<li><strong><code>push(pathname, options?)</code></strong>: Navigates deeper into a child route tree using relative paths (e.g., <code>&quot;./posts&quot;</code>). Requires parent route to end with <code>*</code>.</li>\n<li><strong><code>pop(options?)</code></strong>: Navigates back out of a child route toward its parent by stripping the tail segment.</li>\n<li><strong><code>goback()</code></strong>: Equivalent to <code>history.back()</code>.</li>\n<li><strong><code>link(pathname?)</code></strong>: Builds a relative URL against the current route without navigating.</li>\n<li><strong><code>hasFocus()</code></strong>: Returns <code>true</code> when the current route has child routes but no active tail.</li>\n</ul>\n<h2>Events</h2>\n<ul>\n<li><strong><code>Navigation.event</code></strong>: Dispatched on the host element whenever route state changes. Detail contains <code>prev</code> and <code>next</code> snapshots.</li>\n</ul>\n"
      },
      {
        "id": "route-lifecycle",
        "title": "Route Lifecycle Signatures",
        "order": 3,
        "category": "reference",
        "html": "<p>Every route can declare <code>enter</code>, <code>leave</code>, and <code>render</code>. </p>\n<h3><code>enter</code> Callback</h3>\n<p>Runs before the route becomes active. Return <code>false</code> to cancel navigation.</p>\n<pre><code class=\"language-typescript\">enter: (context: {\n  params: Record&lt;string, string&gt;,\n  extraParams: Record&lt;string, any&gt;,\n  searchParams: Record&lt;string, string&gt;,\n  hash: string,\n  signal: AbortSignal,\n  pathname: string,\n  route: Route,\n}) =&gt; Promise&lt;boolean | void&gt; | boolean | void\n</code></pre>\n<h3><code>leave</code> Callback</h3>\n<p>Runs before navigating away. Return <code>false</code> to cancel.</p>\n<pre><code class=\"language-typescript\">leave: (context: {\n  params: Record&lt;string, string&gt;,\n  extraParams: Record&lt;string, any&gt;,\n  searchParams: Record&lt;string, string&gt;,\n  hash: string,\n  signal: AbortSignal,\n}) =&gt; Promise&lt;boolean | void&gt; | boolean | void\n</code></pre>\n<h3><code>render</code> Callback</h3>\n<p>Fires after <code>enter</code> succeeds.</p>\n<pre><code class=\"language-typescript\">render: (context: {\n  params: Record&lt;string, string&gt;,\n  extraParams: Record&lt;string, any&gt;,\n  searchParams: Record&lt;string, string&gt;,\n  hash: string,\n  route: Route,\n}) =&gt; TemplateResult | nothing | null\n</code></pre>\n"
      },
      {
        "id": "route-matching-syntax",
        "title": "Route Matching Syntax",
        "order": 4,
        "category": "reference",
        "html": "<table>\n<thead>\n<tr>\n<th>Pattern</th>\n<th>Matches</th>\n<th>Example</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>static</strong></td>\n<td>Exact literal segment</td>\n<td><code>/about</code> matches <code>/about</code></td>\n</tr>\n<tr>\n<td><strong><code>:param</code></strong></td>\n<td>Single segment</td>\n<td><code>/users/:id</code> matches <code>/users/42</code></td>\n</tr>\n<tr>\n<td><strong><code>:param?</code></strong></td>\n<td>Zero or one optional segment</td>\n<td><code>/search/:query?</code> matches <code>/search/term</code></td>\n</tr>\n<tr>\n<td><strong><code>*</code></strong></td>\n<td>Zero or more trailing segments (Catch-all)</td>\n<td><code>/admin/*</code> matches <code>/admin/settings</code></td>\n</tr>\n<tr>\n<td><strong><code>:param*</code></strong></td>\n<td>Named capture of tail</td>\n<td><code>/files/:path*</code> captures <code>docs/readme.md</code> into <code>params.path</code></td>\n</tr>\n</tbody></table>\n<p><em>(Note: Named wildcards are not compatible with nested structural routing. Use plain <code>*</code> for DOM-based nesting).</em></p>\n"
      }
    ]
  },
  "how-to": {
    "order": "2",
    "title": "How to use",
    "description": "how to use the router in deep",
    "sections": [
      {
        "id": "manage-anchor-links",
        "title": "How to Manage Anchor Links",
        "order": 1,
        "category": "how-to",
        "html": "<h2>Basic Usage</h2>\n<p>Zero configuration is required for standard routing. Drop <code>&lt;a&gt;</code> elements anywhere in your templates, and <code>lit-router</code> will handle them.</p>\n<pre><code class=\"language-html\">&lt;nav&gt;\n  &lt;a href=&quot;/&quot;&gt;Home&lt;/a&gt;\n  &lt;a href=&quot;/users&quot;&gt;Users&lt;/a&gt;\n&lt;/nav&gt;\n</code></pre>\n<p>Each click navigates to the target route, updates the URL bar, and scrolls to the top without a page reload.</p>\n<h2>Passing Query Parameters and Hashes</h2>\n<p>The Router parses <code>?query</code> and <code>#hash</code> directly from the <code>href</code> attribute.</p>\n<pre><code class=\"language-html\">&lt;a href=&quot;/search?q=lit+router&quot;&gt;Search results for &quot;lit router&quot;&lt;/a&gt;\n&lt;a href=&quot;/docs#lifecycle&quot;&gt;Jump to lifecycle section&lt;/a&gt;\n</code></pre>\n<p>Both are accessible in the target route&#39;s <code>render</code> callback via the <code>searchParams</code> and <code>hash</code> properties on the context object.</p>\n<h2>Excluding Links with <code>router-ignore</code></h2>\n<p>Add the <code>router-ignore</code> attribute to any <code>&lt;a&gt;</code> that should bypass the Router. The browser will handle the click normally.</p>\n<pre><code class=\"language-html\">&lt;!-- External domains (handled automatically, but can be explicit) --&gt;\n&lt;a router-ignore href=&quot;https://github.com/lit-router&quot;&gt;View on GitHub&lt;/a&gt;\n\n&lt;!-- Downloads --&gt;\n&lt;a router-ignore href=&quot;/api/reports/export&quot; download&gt;Download Report&lt;/a&gt;\n</code></pre>\n<p><em>Note: The <code>target=&quot;_blank&quot;</code> attribute already bypasses the Router automatically.</em></p>\n<h2>Handling Hash Links Within the Same Page</h2>\n<p>Links that point to <code>#section</code> on the current page need <code>router-ignore</code>. Without it, the Router intercepts the click, calls <code>navigate()</code>, and scrolls to the top.</p>\n<pre><code class=\"language-html\">&lt;!-- Correct: Browser scrolls to the #faq element --&gt;\n&lt;a router-ignore href=&quot;#faq&quot;&gt;Frequently Asked Questions&lt;/a&gt;\n</code></pre>\n"
      },
      {
        "id": "nest-routes",
        "title": "How to Nest Routes",
        "order": 2,
        "category": "how-to",
        "html": "<h2>Defining Nested Routes</h2>\n<p>To nest routes, the parent route must end with a wildcard <code>*</code>. The child component owns its own <code>Routes</code> controller to handle the tail of the URL.</p>\n<pre><code class=\"language-typescript\">// 1. Root Router (Parent)\nclass AppRoot extends LitElement {\n  _router = new Router(this, [\n    // IMPORTANT: The wildcard captures the tail group\n    { path: &#39;/admin/*&#39;, render: () =&gt; html`&lt;admin-layout&gt;&lt;/admin-layout&gt;` },\n  ]);\n\n  render() { return html`&lt;main&gt;${this._router.outlet()}&lt;/main&gt;`; }\n}\n\n// 2. Child Routes Controller\nclass AdminLayout extends LitElement {\n  _routes = new Routes(this, [\n    { path: &#39;/&#39;,          render: () =&gt; html`&lt;admin-dashboard&gt;&lt;/admin-dashboard&gt;` },\n    { path: &#39;/users&#39;,     render: () =&gt; html`&lt;admin-users&gt;&lt;/admin-users&gt;` },\n  ]);\n\n  render() { return html`&lt;main&gt;${this._routes.outlet()}&lt;/main&gt;`; }\n}\n</code></pre>\n<h2>Adding Guards to Parent Routes</h2>\n<p>Parent routes can use <code>enter</code> to protect all child routes at once. If the parent&#39;s <code>enter</code> returns <code>false</code>, the children never evaluate.</p>\n<pre><code class=\"language-typescript\">{\n  path: &#39;/admin/*&#39;,\n  enter: () =&gt; isAdmin() || false,\n  render: () =&gt; html`&lt;admin-layout&gt;&lt;/admin-layout&gt;`,\n}\n</code></pre>\n"
      },
      {
        "id": "redirect-routes",
        "title": "How to Redirect Inside Routes",
        "order": 3,
        "category": "how-to",
        "html": "<h2>The Redirect Pattern</h2>\n<p>The correct way to redirect a user inside an <code>enter</code> callback is to call <code>goto()</code> followed by <code>return false</code>. This tells the router to stop processing the current route and prevents the URL bar from updating twice.</p>\n<pre><code class=\"language-typescript\">{\n  path: &#39;/dashboard&#39;,\n  enter: async () =&gt; {\n    if (!auth.isLoggedIn) {\n      await this._router.goto(&#39;/login&#39;);\n      return false; // required — cancels the /dashboard navigation\n    }\n  },\n  render: () =&gt; html`&lt;dashboard-page&gt;&lt;/dashboard-page&gt;`,\n}\n</code></pre>\n"
      }
    ]
  },
  "explanation": {
    "order": "4",
    "title": "Advanced",
    "description": "advanced explanations of how the Router work",
    "sections": [
      {
        "id": "anchor-interception",
        "title": "How Anchor Interception Works",
        "order": 1,
        "category": "explanation",
        "html": "<h2>The Interception Mechanism</h2>\n<p>The <code>Router</code> intercepts all <code>&lt;a&gt;</code> clicks automatically. A single event listener is installed on <code>window</code> in the capture phase. This removes the need for per-link wiring or event delegation boilerplate.</p>\n<p>When a click meets the interception conditions, the Router calls <code>e.preventDefault()</code>, <code>router.navigate()</code>, and <code>window.scrollTo(0, 0)</code>. </p>\n<h3>Interception Conditions</h3>\n<p>If any of these conditions fail, the browser handles the click natively:</p>\n<table>\n<thead>\n<tr>\n<th>Condition</th>\n<th>Why it must be met</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>e.defaultPrevented</code> is <code>false</code></td>\n<td>Another listener already consumed the event</td>\n</tr>\n<tr>\n<td>Composed path contains an <code>&lt;a&gt;</code></td>\n<td>Clicks on nested children of <code>&lt;a&gt;</code> must bubble</td>\n</tr>\n<tr>\n<td>Anchor has an <code>href</code></td>\n<td>Links without <code>href</code> are not navigable</td>\n</tr>\n<tr>\n<td><code>e.button === 0</code></td>\n<td>Middle/Right-clicks open new tabs</td>\n</tr>\n<tr>\n<td>No modifier keys</td>\n<td>User intent is to open in new tab/window</td>\n</tr>\n<tr>\n<td>No <code>download</code> attribute</td>\n<td>Downloads are handled by the browser</td>\n</tr>\n<tr>\n<td>No <code>router-ignore</code> attribute</td>\n<td>Explicit opt-out from router handling</td>\n</tr>\n<tr>\n<td>Target is not <code>_blank</code>, <code>_parent</code>, etc.</td>\n<td>Cross-context navigation is browser territory</td>\n</tr>\n<tr>\n<td>Same origin as <code>window.location</code></td>\n<td>External domains load normally</td>\n</tr>\n</tbody></table>\n<h2>Anchor Navigation vs Programmatic</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Anchor Links (<code>&lt;a&gt;</code>)</th>\n<th>Programmatic (<code>navigate()</code>)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Trigger</td>\n<td>User click</td>\n<td>Any code path (timeouts, fetch)</td>\n</tr>\n<tr>\n<td>Scroll</td>\n<td><code>scrollTo(0, 0)</code> automatically</td>\n<td>Manual</td>\n</tr>\n<tr>\n<td>Error handling</td>\n<td><code>@lit-router-error</code> event</td>\n<td><code>try/catch</code> block</td>\n</tr>\n</tbody></table>\n"
      },
      {
        "id": "navigation-flow",
        "title": "The Navigation Flow",
        "order": 2,
        "category": "explanation",
        "html": "<h2>Lifecycle Order</h2>\n<p>When navigating from one route to another (e.g., <code>/editor/1</code> to <code>/dashboard</code>), the router processes hooks in a specific order:</p>\n<ol>\n<li><strong>Leave</strong> of current route fires (deepest child → ancestors).</li>\n<li><strong>Enter</strong> of target route fires.</li>\n<li>If any callback returns <code>false</code>, navigation is cancelled.</li>\n<li>If all callbacks pass, the new route renders and window history updates.</li>\n</ol>\n<pre class=\"mermaid\">sequenceDiagram\n    participant U as User\n    participant R as Router\n    participant L as Leave Callback\n    participant E as Enter Callback\n    participant P as Render\n\n    U->>R: Navigate to /dashboard\n    R->>L: Run leave on current route\n    L-->>R: proceed\n    R->>E: Run enter on /dashboard\n    alt enter succeeds\n        E-->>R: proceed\n        R->>P: Render matched component\n    else enter returns false\n        E-->>R: cancel\n    end</pre>\n<h2><code>goto</code> vs <code>navigate</code></h2>\n<p>While both resolve a pathname and activate a route, their impact on the browser differs:</p>\n<ul>\n<li><strong><code>navigate()</code></strong>: Triggers <code>history.pushState</code> and updates the URL bar. Used for user-facing navigation.</li>\n<li><strong><code>goto()</code></strong>: Does <strong>not</strong> touch the URL bar. Used internally for redirects inside <code>enter</code> callbacks, ensuring the browser history isn&#39;t polluted with aborted routes.</li>\n</ul>\n"
      },
      {
        "id": "route-scoring",
        "title": "Route Scoring & Precedence",
        "order": 3,
        "category": "explanation",
        "html": "<h2>How Routes Are Scored</h2>\n<p><code>lit-router</code> assigns a numeric score to every route at construction time. Routes are sorted descending by score, so <strong>the most specific route is tried first</strong>. The order in which you define them in the array does not matter.</p>\n<table>\n<thead>\n<tr>\n<th>Segment type</th>\n<th>Weight</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Static</td>\n<td>1000</td>\n</tr>\n<tr>\n<td>Dynamic (<code>:param</code>)</td>\n<td>100</td>\n</tr>\n<tr>\n<td>Optional (<code>:param?</code>)</td>\n<td>10</td>\n</tr>\n<tr>\n<td>Depth (per segment)</td>\n<td>1</td>\n</tr>\n<tr>\n<td>Wildcard (<code>*</code>)</td>\n<td>-50</td>\n</tr>\n</tbody></table>\n<h2>Scoring in Practice</h2>\n<ol>\n<li><strong>Static beats dynamic</strong>: <code>/orders/list</code> (score ~2002) beats <code>/orders/:id</code> (score ~1102). </li>\n<li><strong>Deep routes outscore shallow ones</strong>: <code>/blog/:year/:month</code> (score ~203) beats <code>/blog/:slug</code> (score ~102).</li>\n<li><strong>Wildcard is the last resort</strong>: Because <code>*</code> carries a negative penalty, a catch-all route <code>{ path: &#39;*&#39; }</code> will always be evaluated last, making it the perfect 404 handler.</li>\n</ol>\n"
      },
      {
        "id": "nested-routing",
        "title": "Architecture of Nested Routing",
        "order": 4,
        "category": "explanation",
        "html": "<h2>Structural DOM Nesting</h2>\n<p><code>lit-router</code> avoids giant, centralized configuration objects. Instead, nesting is structural. A parent <code>Router</code> delegates the unmatched portion of the URL to a child <code>Routes</code> controller sitting inside its DOM outlet. </p>\n<h3>How Tail Propagation Works</h3>\n<p>A route ending with <code>*</code> captures everything after the matched static prefix. This unmatched portion is called the <strong>tail group</strong>.</p>\n<pre><code class=\"language-text\">URL: /admin/users/42\n\nRouter matches /admin/*  →  tail group = &quot;/users/42&quot;\n  └─ Renders &lt;admin-layout&gt;\n       └─ Child Routes matches /users/:id  →  params.id = &quot;42&quot;\n            └─ Renders &lt;user-detail id=&quot;42&quot;&gt;\n</code></pre>\n<h3>The Missing Wildcard Mistake</h3>\n<p>A common mistake is omitting the <code>*</code> on the parent route. Without it, the router assumes an exact static match. The tail group is never created, and the child <code>&lt;admin-layout&gt;</code> never receives the remaining URL segments to parse.</p>\n<pre><code class=\"language-typescript\">// Wrong — /admin/settings fails because exact match is required\n{ path: &#39;/admin&#39;, render: () =&gt; html`&lt;admin-layout&gt;&lt;/admin-layout&gt;` }\n\n// Correct — wildcard captures the tail for the child routes\n{ path: &#39;/admin/*&#39;, render: () =&gt; html`&lt;admin-layout&gt;&lt;/admin-layout&gt;` }\n</code></pre>\n"
      }
    ]
  }
};
